# need to rebuild the model just from the weights 
# needto rebuild the model structure 
# load the learned weights 

import pickle, numpy as np 

class Predictor: 
    def __init__(self, model_path, scaler_path=None):
        pass